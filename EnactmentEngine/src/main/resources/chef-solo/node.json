{
        "run_list" : [
        		"recipe[ntp]",
        		"recipe[sysstat]"
        ],
        "ganglia": {
        	"cluster_name": "CHOReOS",
        	"unicast": true,
        	"server_addresses": [ "localhost" ]
		}
}
